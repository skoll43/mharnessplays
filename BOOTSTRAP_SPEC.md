# BOOTSTRAP_SPEC

This repository bootstraps a pixel-only dual-loop architecture with:

- deterministic in-band control scaffolding
- asynchronous meta-loop proposal pipeline (proposal-only)
- strict safety boundaries between planning and execution

Implementation follows first-target order and enforces bootstrap runtime invariants.
